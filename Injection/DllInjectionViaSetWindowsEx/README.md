# Dll Injection via SetWindowsEx

## Usage

### 1. Compile HookDll

Build [HookDll](HookDll).

### 2. Set the DLL Path

In `DllInjectionViaSetWindowsEx.cpp`, set the DLL path and the exported function name.
